# API mode: "mock" (development default) or "real" (production)
# GET /performances（楽曲データ取得）のみに影響する。
# Controller <-> Viewer のリアルタイム通信は Vite サーバー内の WebSocket リレーで行う。
VITE_API_MODE=mock

# HTTP API Base URL (empty for mock mode - uses relative paths)
VITE_API_BASE_URL=
